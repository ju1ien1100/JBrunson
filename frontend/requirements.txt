# core
pymupdf
aiohttp
numpy
pydantic

# vision analysis (pdf_reader.py --analyze + model_server.py): needs ANTHROPIC_API_KEY
anthropic

# Modal GPU (model_server.py): needs Modal auth + deployed apps
modal

# voice track: transcode Suno's Opus-in-MP4 -> browser-playable MP3 (static ffmpeg)
imageio-ffmpeg
