# core
pymupdf
websockets
numpy

# stage 1 — analyze (pdf_reader.py --analyze): needs ANTHROPIC_API_KEY
anthropic

# stage 2 — serve (model_server.py): needs Modal auth + deployed apps + SUNO_API_KEY
modal
aiohttp
